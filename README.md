Проект по классификации рукописных цифр (датасет MNIST) с использованием сверточной нейронной сети (CNN). Модель реализована на фреймворке **PyTorch** и достигает точности **99.1%** на тестовой выборке.

## 📋 Особенности реализации
Проект демонстрирует полный цикл разработки ML-модели:
* **Предобработка данных:** Нормализация, приведение размерности, создание кастомных `DataLoader`.
* **Архитектура CNN:** 2 сверточных слоя (Conv2d), функции активации ReLU, Max Pooling, Dropout (для регуляризации) и полносвязные слои.
* **Обучение:** Использование оптимизатора Adam и функции потерь CrossEntropyLoss.
* **Глубокая аналитика:** Оценка качества не только по Accuracy, но и по Precision, Recall, F1-score (Micro/Macro averaging).
* **Визуализация:** Построение матрицы ошибок (Confusion Matrix) для анализа ложных срабатываний.

## 🛠 Технологический стек
* **Core:** Python
* **Deep Learning:** PyTorch (`torch`, `torch.nn`, `torch.utils.data`)
* **Data Manipulation:** Pandas, NumPy
* **Visualization:** Matplotlib, Seaborn
* **Metrics:** Scikit-learn

## 🚀 Как запустить

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/goldenserzh/number_detector.git
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Запустите Jupyter Notebook:**
    ```bash
    jupyter notebook лаб_1_ИИ.ipynb
    ```

## 📊 Результаты
Модель обучалась 5 эпох. Итоговые метрики на тестовой выборке:

| Metric | Value |
|ssd|ssd|
| **Accuracy** | **0.991** |
| F1 Score (Macro) | 0.991 |
| Precision (Macro)| 0.991 |

### Архитектура модели
```python
ConvModel(
  (layer_1): Conv2d -> ReLU -> MaxPool2d
  (layer_2): Conv2d -> ReLU -> MaxPool2d
  (drop_out): Dropout(p=0.5)
  (fc1): Linear(in=7*7*64, out=1000)
  (fc2): Linear(in=1000, out=10)
)
